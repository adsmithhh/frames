This directory mirrors the local "experiment configurations" tree.

Excluded from commit:
- Provider API key files (see *.example placeholders)
- gemini/record.txt (log)

To use providers, set environment variables or replace the
corresponding *.example files locally without committing real keys.
